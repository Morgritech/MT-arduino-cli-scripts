# MT-arduino-cli-scripts
Setup and build scripts leveraging Arduino CLI for compiling and uploading Arduino C++ code on various operating systems (including Windows and Linux) without requiring an IDE.
